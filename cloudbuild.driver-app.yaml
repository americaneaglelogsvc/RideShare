steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/urway-driver:latest
      - --build-arg
      - VITE_API_BASE_URL=https://api.urwaydispatch.com
      - .
images:
  - gcr.io/$PROJECT_ID/urway-driver:latest
